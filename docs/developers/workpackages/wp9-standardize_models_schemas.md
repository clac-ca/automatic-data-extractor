# WP9 — Standardize Models & Schemas (Deprecated)

This work package referenced legacy build schemas. Builds have been removed;
the system now centers on runs + environments.

See `apps/ade-worker/workpackages/ade-worker-rebuild/` for current schema work.

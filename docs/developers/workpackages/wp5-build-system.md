# WP5 — Environment System (Deprecated)

This work package targeted the old build orchestration system. Builds have been
removed from the API; environments are now worker-owned and provisioned on
run start.

See `apps/ade-worker/workpackages/ade-worker-rebuild/` for the current plan and
implementation details.

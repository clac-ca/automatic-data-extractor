# WP9 — Contract Audit (Deprecated)

This audit predates the runs + environments model and references build routes.
The build API surface has been removed.

See `apps/ade-worker/workpackages/ade-worker-rebuild/` for current contracts.

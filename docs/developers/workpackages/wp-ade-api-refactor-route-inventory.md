# ADE API Route Inventory (Deprecated)

This route inventory predates the removal of build orchestration. Build routes
have been removed from the API, so the original list is no longer accurate.

Use the OpenAPI spec (`apps/ade-api/src/ade_api/openapi.json`) as the source of
truth for current routes.

# WP10 — Standard DB IDs (Deprecated)

This work package referenced legacy build identifiers. The build system has
been removed in favor of environments + runs.

See `apps/ade-worker/workpackages/ade-worker-rebuild/` for current DB ID work.

# WP6 — Runs Integration (Deprecated)

This work package referenced the legacy build queue and build identifiers.
The system now uses runs + environments only.

See `apps/ade-worker/workpackages/ade-worker-rebuild/` for the current plan.

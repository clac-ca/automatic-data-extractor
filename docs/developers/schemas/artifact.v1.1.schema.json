{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:ade:artifact.v1.1",
  "title": "ADE Artifact v1.1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema",
    "artifact_version",
    "run",
    "config",
    "engine",
    "rules",
    "sheets",
    "output",
    "summary",
    "pass_history",
    "annotations"
  ],
  "properties": {
    "schema": { "type": "string", "const": "ade.artifact/v1" },
    "artifact_version": { "type": "string", "const": "1.1" },

    "run": {
      "type": "object",
      "additionalProperties": false,
      "required": ["run_id", "status", "started_at", "completed_at", "source_file"],
      "properties": {
        "run_id": { "type": "string", "minLength": 1 },
        "config_version_id": { "type": ["string", "null"] },
        "status": { "type": "string", "enum": ["succeeded", "failed"] },
        "started_at": { "type": "string", "format": "date-time" },
        "completed_at": { "type": "string", "format": "date-time" },
        "source_file": { "type": "string", "minLength": 1 }
      }
    },

    "config": {
      "type": "object",
      "additionalProperties": false,
      "required": ["config_version_id"],
      "properties": {
        "config_version_id": { "type": ["string", "null"] },
        "manifest_version": { "type": ["string", "null"] }
      }
    },

    "engine": {
      "type": "object",
      "additionalProperties": false,
      "required": ["writer"],
      "properties": {
        "writer": {
          "type": "object",
          "additionalProperties": false,
          "required": ["mode", "append_unmapped_columns", "unmapped_prefix"],
          "properties": {
            "mode": { "type": "string", "enum": ["row_streaming", "in_memory"] },
            "append_unmapped_columns": { "type": "boolean", "default": true },
            "unmapped_prefix": { "type": "string", "default": "raw_" },
            "output_sheet": { "type": ["string", "null"] }
          }
        },
        "defaults": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "timeout_ms": { "type": "integer", "minimum": 0 },
            "memory_mb": { "type": "integer", "minimum": 0 },
            "runtime_network_access": { "type": "boolean" },
            "mapping_score_threshold": { "type": "number" }
          }
        }
      }
    },

    "rules": {
      "type": "object",
      "additionalProperties": false,
      "required": ["row_types", "column_detect", "transform", "validate"],
      "properties": {
        "row_types": { "$ref": "#/$defs/RuleRegistry" },
        "column_detect": { "$ref": "#/$defs/RuleRegistry" },
        "transform": { "$ref": "#/$defs/RuleRegistry" },
        "validate": { "$ref": "#/$defs/RuleRegistry" }
      }
    },

    "sheets": {
      "type": "array",
      "items": { "$ref": "#/$defs/Sheet" }
    },

    "output": {
      "oneOf": [
        { "type": "null" },
        { "$ref": "#/$defs/OutputPlan" }
      ]
    },

    "summary": {
      "oneOf": [
        { "type": "null" },
        { "$ref": "#/$defs/RunSummary" }
      ]
    },

    "pass_history": {
      "type": "array",
      "items": { "$ref": "#/$defs/PassHistoryItem" }
    },

    "annotations": {
      "type": "array",
      "items": { "$ref": "#/$defs/Annotation" }
    }
  },

  "$defs": {
    "IdSheet": { "type": "string", "pattern": "^sheet_[1-9][0-9]*$" },
    "IdTable": { "type": "string", "minLength": 1 },
    "IdColumn": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_.\-]+\\.col\\.[1-9][0-9]*$"
    },
    "TargetFieldId": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },

    "RuleId": {
      "type": "string",
      "description": "Dotted module id with callable suffix, e.g. columns.member_id:transform",
      "pattern": "^[a-z][a-z0-9_]*(?:\\.[a-z0-9_]+)*:[a-z_][a-z0-9_]*$"
    },

    "ImplRef": {
      "type": "string",
      "description": "Path-with-function 'path/to/file.py:function_name'",
      "pattern": "^[A-Za-z0-9_\\-./]+:[A-Za-z_][A-Za-z0-9_]*$"
    },

    "A1Cell": {
      "type": "string",
      "pattern": "^[A-Z]{1,3}[1-9][0-9]*$"
    },

    "A1Range": {
      "type": "string",
      "pattern": "^[A-Z]{1,3}[1-9][0-9]*:[A-Z]{1,3}[1-9][0-9]*$"
    },

    "RuleRegistry": {
      "type": "object",
      "additionalProperties": false,
      "patternProperties": {
        "^[a-z][a-z0-9_]*(?:\\.[a-z0-9_]+)+$": {
          "type": "object",
          "additionalProperties": false,
          "required": ["impl"],
          "properties": {
            "impl": { "$ref": "#/$defs/ImplRef" },
            "field": { "$ref": "#/$defs/TargetFieldId" },
            "description": { "type": "string" }
          }
        }
      }
    },

    "RuleTrace": {
      "type": "object",
      "additionalProperties": false,
      "required": ["rule", "delta"],
      "properties": {
        "rule": { "$ref": "#/$defs/RuleId" },
        "delta": { "type": "number" }
      }
    },

    "RowClassItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["row_index", "label", "scores_by_type"],
      "properties": {
        "row_index": { "type": "integer", "minimum": 1 },
        "label": { "type": "string", "enum": ["header", "data", "separator", "group_header"] },
        "confidence": { "type": "number" },
        "scores_by_type": {
          "type": "object",
          "additionalProperties": { "type": "number" }
        },
        "rule_traces": {
          "type": "array",
          "items": { "$ref": "#/$defs/RuleTrace" }
        }
      }
    },

    "HeaderInfo": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "row_index", "source_header"],
      "properties": {
        "kind": { "type": "string", "enum": ["raw", "synthetic"] },
        "row_index": { "type": "integer", "minimum": 1 },
        "source_header": {
          "type": "array",
          "items": { "type": ["string", "null"] },
          "minItems": 1
        }
      }
    },

    "ColumnDescriptor": {
      "type": "object",
      "additionalProperties": false,
      "required": ["column_id", "order"],
      "properties": {
        "column_id": { "$ref": "#/$defs/IdColumn" },
        "source_header": { "type": ["string", "null"] },
        "order": { "type": "integer", "minimum": 1 }
      }
    },

    "Contributor": {
      "type": "object",
      "additionalProperties": false,
      "required": ["rule", "delta"],
      "properties": {
        "rule": { "$ref": "#/$defs/RuleId" },
        "delta": { "type": "number" }
      }
    },

    "MappingEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["raw", "target_field", "score"],
      "properties": {
        "raw": {
          "type": "object",
          "additionalProperties": false,
          "required": ["column"],
          "properties": {
            "column": { "$ref": "#/$defs/IdColumn" },
            "header": { "type": ["string", "null"] }
          }
        },
        "target_field": {
          "oneOf": [
            { "$ref": "#/$defs/TargetFieldId" },
            { "type": "null" }
          ]
        },
        "score": { "type": "number" },
        "contributors": {
          "type": "array",
          "items": { "$ref": "#/$defs/Contributor" }
        }
      }
    },

    "FieldTinyStats": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "distinct": { "type": "integer", "minimum": 0 },
        "empty": { "type": "integer", "minimum": 0 },
        "mostly_alnum": { "type": "boolean" },
        "mostly_lower": { "type": "boolean" }
      }
    },

    "TransformSummary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["target_field", "transform", "changed", "total"],
      "properties": {
        "target_field": { "$ref": "#/$defs/TargetFieldId" },
        "transform": { "$ref": "#/$defs/RuleId" },
        "changed": { "type": "integer", "minimum": 0 },
        "total": { "type": "integer", "minimum": 0 },
        "warnings": {
          "type": "array",
          "items": { "type": "string" }
        },
        "notes": { "type": "string" }
      }
    },

    "ValidationIssue": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "severity", "message"],
      "properties": {
        "a1": { "$ref": "#/$defs/A1Cell" },
        "row_index": { "type": "integer", "minimum": 1 },
        "target_field": { "$ref": "#/$defs/TargetFieldId" },
        "code": { "type": "string" },
        "severity": { "type": "string", "enum": ["error", "warning", "info"] },
        "message": { "type": "string" },
        "column": { "$ref": "#/$defs/IdColumn" },
        "rule": { "$ref": "#/$defs/RuleId" }
      }
    },

    "ValidationBlock": {
      "type": "object",
      "additionalProperties": false,
      "required": ["issues", "summary_by_field"],
      "properties": {
        "issues": {
          "type": "array",
          "items": { "$ref": "#/$defs/ValidationIssue" }
        },
        "summary_by_field": {
          "type": "object",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "errors": { "type": "integer", "minimum": 0 },
              "warnings": { "type": "integer", "minimum": 0 },
              "missing": { "type": "integer", "minimum": 0 }
            }
          }
        }
      }
    },

    "OutputPlan": {
      "type": "object",
      "additionalProperties": false,
      "required": ["format", "sheet", "path", "column_plan"],
      "properties": {
        "format": { "type": "string", "enum": ["xlsx", "csv", "parquet"] },
        "sheet": { "type": "string", "minLength": 1 },
        "path": { "type": "string", "minLength": 1 },
        "column_plan": {
          "type": "object",
          "additionalProperties": false,
          "required": ["target", "appended_unmapped"],
          "properties": {
            "target": {
              "type": "array",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": ["field", "output_header", "order"],
                "properties": {
                  "field": { "$ref": "#/$defs/TargetFieldId" },
                  "output_header": { "type": "string" },
                  "order": { "type": "integer", "minimum": 1 }
                }
              }
            },
            "appended_unmapped": {
              "type": "array",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": ["source_header", "output_header", "order", "column"],
                "properties": {
                  "source_header": { "type": ["string", "null"] },
                  "output_header": { "type": "string" },
                  "order": { "type": "integer", "minimum": 1 },
                  "column": { "type": "string" }
                }
              }
            }
          }
        }
      }
    },

    "RunSummary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["rows_written", "columns_written", "issues_found"],
      "properties": {
        "rows_written": { "type": "integer", "minimum": 0 },
        "columns_written": { "type": "integer", "minimum": 0 },
        "issues_found": { "type": "integer", "minimum": 0 }
      }
    },

    "PassHistoryItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pass", "name", "completed_at"],
      "properties": {
        "pass": { "type": "integer", "minimum": 1 },
        "name": {
          "type": "string",
          "enum": ["structure", "mapping", "transform", "validate", "generate"]
        },
        "completed_at": { "type": "string", "format": "date-time" },
        "stats": {
          "type": "object",
          "additionalProperties": { "type": "integer", "minimum": 0 }
        }
      }
    },

    "Annotation": {
      "type": "object",
      "required": ["stage", "hook", "annotated_at"],
      "properties": {
        "stage": { "type": "string" },
        "hook": { "type": "string" },
        "annotated_at": { "type": "string", "format": "date-time" }
      },
      "additionalProperties": true
    },

    "Table": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "range", "header", "columns"],
      "properties": {
        "id": { "$ref": "#/$defs/IdTable" },
        "range": { "$ref": "#/$defs/A1Range" },
        "data_range": {
          "oneOf": [
            { "type": "null" },
            { "$ref": "#/$defs/A1Range" }
          ]
        },
        "header": { "$ref": "#/$defs/HeaderInfo" },
        "columns": {
          "type": "array",
          "items": { "$ref": "#/$defs/ColumnDescriptor" }
        },
        "mapping": {
          "type": "array",
          "items": { "$ref": "#/$defs/MappingEntry" }
        },
        "analyze": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/FieldTinyStats" }
        },
        "transforms": {
          "type": "array",
          "items": { "$ref": "#/$defs/TransformSummary" }
        },
        "validation": { "$ref": "#/$defs/ValidationBlock" }
      }
    },

    "Sheet": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "tables"],
      "properties": {
        "id": { "$ref": "#/$defs/IdSheet" },
        "name": { "type": "string" },
        "row_classification": {
          "type": "array",
          "items": { "$ref": "#/$defs/RowClassItem" }
        },
        "tables": {
          "type": "array",
          "items": { "$ref": "#/$defs/Table" }
        }
      }
    }
  }
}

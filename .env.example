# -----------------------------------------------------------------------------
# ADE environment template
# Copy to `.env` and tweak values as needed.
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Core backend settings (defaults mirror Settings model)
# -----------------------------------------------------------------------------
ADE_DEBUG=false
ADE_ENABLE_DOCS=false
ADE_LOG_LEVEL=INFO
ADE_AUTH_TOKEN_SECRET=development-secret
ADE_DATA_DIR=backend/data
ADE_MAX_UPLOAD_BYTES=26214400
ADE_DEFAULT_DOCUMENT_RETENTION_DAYS=30

# Allowed origins for local development CORS (comma separated list)
ADE_CORS_ALLOW_ORIGINS=

# -----------------------------------------------------------------------------
# Frontend development
# -----------------------------------------------------------------------------
# Vite reads this value when you run `npm run dev`; keep it in sync with the
# FastAPI host+port you start locally (typically 127.0.0.1:8000).
VITE_API_BASE_URL=http://127.0.0.1:8000

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# ADE_DATABASE_URL defaults to the bundled SQLite database; uncomment to point at
# PostgreSQL or a different file path when you deploy.
# ADE_DATABASE_URL=sqlite+aiosqlite:///./backend/data/db/ade.sqlite

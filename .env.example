# ADE environment variables
# Copy only when needed: cp .env.example .env
# Local quickstart usually needs no .env: docker compose up --build -d
# Keep .env for ADE runtime settings.
# Compose-only/dependency variables are intentionally excluded.

# Core production settings.
# ADE_PUBLIC_WEB_URL=https://ade.example.com                        # public base URL
# ADE_DATABASE_URL=postgresql+psycopg://user:pass@db.example.com:5432/ade?sslmode=verify-full
# ADE_SECRET_KEY=replace-with-long-random-secret                    # >=32 bytes; 64+ recommended
# ADE_BLOB_CONTAINER=ade
# ADE_BLOB_ACCOUNT_URL=https://<account>.blob.core.windows.net      # set exactly one blob auth method (not both)
# ADE_BLOB_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=<account>;AccountKey=<key>;EndpointSuffix=core.windows.net

# Local overrides (optional).
# ADE_AUTH_DISABLED=true              # true|false; local compose default is true
# ADE_SERVICES=api,worker,web         # local app services: api,worker,web
# ADE_DB_MIGRATE_ON_START=true        # true|false
# ADE_PUBLIC_WEB_URL=http://localhost:8000

# Production topology and safety.
# Use docker-compose.prod.yaml for single-container mode (api+worker+web).
# Use docker-compose.prod.split.yaml for split mode (app + worker).
# ADE_AUTH_DISABLED=false             # never enable in production
# ADE_SAFE_MODE=false                 # true blocks engine execution paths

# Database and blob options (optional).
# ADE_DATABASE_AUTH_MODE=password     # password|managed_identity
# ADE_DATABASE_SSLROOTCERT=/path/to/ca.crt
# ADE_BLOB_PREFIX=workspaces
# ADE_BLOB_VERSIONING_MODE=auto       # auto|require|off (ADE behavior only; Azure account setting is separate)
# ADE_DATA_DIR=/var/lib/ade/data

# Logging.
# ADE_LOG_LEVEL=INFO                  # DEBUG|INFO|WARNING|ERROR|CRITICAL
# ADE_LOG_FORMAT=console              # console|json

# Performance/tuning (optional).
# ADE_API_WORKERS=1                  # API processes per container (increase for API-bound load)
# ADE_WORKER_CONCURRENCY=2           # run slots per worker container (increase for queue throughput)
# ADE_WORKER_LEASE_SECONDS=900
# ADE_WORKER_ENV_BUILD_TIMEOUT_SECONDS=600
# ADE_WORKER_RUN_TIMEOUT_SECONDS=
# ADE_BLOB_MAX_CONCURRENCY=4
# ADE_BLOB_UPLOAD_CHUNK_SIZE_BYTES=4194304
# ADE_BLOB_DOWNLOAD_CHUNK_SIZE_BYTES=1048576
# ADE_SERVER_CORS_ORIGINS=["https://app.example.com"]

# Full variable catalog: docs/reference/env-vars.md

# Backend Scripts

Utilities in this directory are Python CLIs that depend on the FastAPI app code and settings. They provide developer tooling (e.g. route summaries, storage resets) that should run inside the backend virtual environment so they can import `ade_api` modules safely.

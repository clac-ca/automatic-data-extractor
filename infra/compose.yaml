services:
  ade:
    build:
      context: ..
      dockerfile: infra/docker/api.Dockerfile
    image: ade:local
    ports:
      - "8000:8000"
    volumes:
      - ade-data:/app/data

volumes:
  ade-data:

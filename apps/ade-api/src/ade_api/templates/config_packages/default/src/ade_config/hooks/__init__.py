"""Hook modules that extend the engine at well-defined stages.

Each file here is referenced by the `hooks` section of `manifest.json`
and exposes a single `run(ctx)` function, where `ctx` is a HookContext
from `ade_engine`.
"""

__all__ = []

"""Column modules for each canonical field.

Each module typically exposes:
  - one or more `detect_*` functions,
  - an optional `transform` function,
  - an optional `validate` function.
"""

__all__ = []
